# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Local env
.env
.env.local
.env.*.local

# spec-workflow MCP local state (本地工作流工具状态, 不入库)
.spec-workflow/

# Local Claude session state
.claude-sessions.json

# headroom learn 自动生成的命令笔记 (含个人机绝对路径, 跨机器无意义).
# 工具 0.24.0 误把私域学习状态写到团队级 CLAUDE.md, 待上游支持 CLAUDE.local.md 后可移除.
CLAUDE.md
# CLAUDE.local.md 本身也是 headroom 每次命令后自动重写的本机学习状态,
# 曾被误提交入库(c5576dc), 导致 pull --rebase 反复因 dirty tree 失败. 不应追踪.
CLAUDE.local.md
.superpowers/

# 阈值校准原始收盘缓存 (22M, 本机复用, 勿入库)
backend/research/breadth-calibration/closes_full.json.gz
# Coverage 产物 (diff-cover pre-push 钩子生成)
backend/coverage.xml
frontend/coverage/
frontend/lcov.info

# Added by code-review-graph
.code-review-graph/
# SEPA 回测指数缓存（本地拉取产物，不入库）
backend/scripts/research/cache/

# 维护工厂运行时产物
.factory/artifacts/
.factory/locks/
.factory/worktrees/
