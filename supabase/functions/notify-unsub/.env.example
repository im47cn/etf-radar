# notify-unsub Edge Function 运行时密钥（在 Supabase secrets 中配置，切勿提交真实值）
# 通过 `supabase secrets set KEY=VALUE` 注入，函数内仅以 Deno.env.get 读取。

# Supabase 项目 URL（形如 https://xxxx.supabase.co）
SUPABASE_URL=

# Supabase service_role key（绕过 RLS 按 token 写 notify_prefs，务必保密，切勿下发前端）
SUPABASE_SERVICE_ROLE_KEY=
